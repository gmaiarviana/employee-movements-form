# Frontend Configuration
VITE_PORT=3001
# Use backend service name for Docker networking, localhost for local development
VITE_API_URL=http://backend:3000

# Development Configuration
NODE_ENV=development

# Proxy Configuration
VITE_PROXY_TIMEOUT=10000
